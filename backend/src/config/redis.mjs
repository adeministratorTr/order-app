export const REDIS_CONFIG = {
  HOST: 'redis-12481.c135.eu-central-1-1.ec2.cloud.redislabs.com',
  // Normally, PORT value should come from secrets configuration, not hardcoded.
  // But since, its a challenge, I didnt implement any configration structure
  PORT: '12481',
  // PASSWORD also should come from secrets configuration, not hardcoded
  PASSWORD: 'LYew9Z3Onc0HUI1K17F4WjMydBHnFPJS'
}
